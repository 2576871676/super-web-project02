<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.king.mapper.ClazzMapper">
    <!--条件分页查询班级列表（带班级名称模糊搜索、结课时间区间筛选，关联员工表取班主任姓名）-->
    <select id="list" resultType="org.king.pojo.Clazz">
        select c.*, e.name masterName
        from clazz c left join emp e on c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                and c.name like concat('%', #{name}, '%')
            </if>
            <if test="begin != null">
                and c.end_date &gt;= #{begin}
            </if>
            <if test="end != null">
                and c.end_date &lt;= #{end}
            </if>
        </where>
        order by c.update_time desc
    </select>
</mapper>
